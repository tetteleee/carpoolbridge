# Task T24a イベント編集 回答入力画面 ルーティング追加

---

## 1. 対象設計書

ref:
- docs/04_画面設計.md#4 画面遷移（`C -->|回答編集| D`、`D -->|戻る| C`、`D -->|配車作成| C`）
- docs/04_画面設計.md#7 イベント編集 回答入力

---

## 2. このタスクのゴール

イベント編集 回答入力画面（T24で実装開始）をURLから直接開ける状態にする。

遷移元となる配車画面（メイン）はT38以降で実装されるため、配車画面（メイン）との往復導線は本タスクでは接続しない（T39aで実施）。

---

## 3. 変更対象ファイル（想定）

- `src/router/index.tsx`
- `src/pages/EventEditPage.tsx`

※ファイル構成はプロジェクト構成に合わせて読み替えてよい。

---

## 4. 実装範囲（やること）

- イベント編集 回答入力画面（`EventEditPage.tsx`）へのルートを定義し、対象イベントを指定してURLから直接開ける状態にする（例: イベントIDをパスパラメータとして受け取る）

---

## 5. 実装範囲外（やらないこと）

- イベント編集 回答入力画面自体のUI・入力処理（T24〜T31で実施）
- 配車画面（メイン）からの「回答編集」導線、および「戻る」「配車作成」による配車画面（メイン）への遷移（配車画面メインが未実装のため対象外。T39aで実施）
- 配車画面（メイン）のルーティング（T38aで実施）

---

## 6. 受け入れ条件

- イベントIDを指定したURLに直接アクセスすると、そのイベントのイベント編集 回答入力画面が表示される

---

## 7. 依存タスク

- T24 家庭カード一覧のレイアウト・データ取得

---

## 提案（タスク対象外）

なし
