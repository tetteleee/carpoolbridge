# Task T38a 配車画面（メイン） ルーティング追加

---

## 1. 対象設計書

ref:
- docs/04_画面設計.md#4 画面遷移（`A -->|イベント選択| C`）
- docs/04_画面設計.md#5 ホーム（イベント一覧）（「タップで配車画面へ遷移する」）
- docs/04_画面設計.md#8 配車画面（メイン）

---

## 2. このタスクのゴール

配車画面（メイン）（T38で実装開始）をURLから直接開ける状態にし、T21で保留にしていた「ホーム画面でイベントをタップすると配車画面（メイン）へ遷移する」導線を接続する。

---

## 3. 変更対象ファイル（想定）

- `src/router/index.tsx`
- `src/pages/HomePage.tsx`
- `src/pages/CarpoolPage.tsx`

※ファイル構成はプロジェクト構成に合わせて読み替えてよい。

---

## 4. 実装範囲（やること）

- 配車画面（メイン）（`CarpoolPage.tsx`）へのルートを定義し、対象イベントを指定してURLから直接開ける状態にする（例: イベントIDをパスパラメータとして受け取る）
- ホーム画面のイベント行タップ時に、このルートへ遷移するよう接続する（T21の実装範囲外としていた項目）

---

## 5. 実装範囲外（やらないこと）

- 配車画面（メイン）自体のUI・状態管理（T38〜T44で実施）
- 配車画面（メイン）とイベント編集 回答入力画面との往復導線（T39aで実施）
- 配車画面（メイン）とLINE共有画面との往復導線（T46aで実施。LINE共有画面が未実装のため対象外）
- ホーム画面自体の実装（T21で実施済み）

---

## 6. 受け入れ条件

- イベントIDを指定したURLに直接アクセスすると、そのイベントの配車画面（メイン）が表示される
- ホーム画面でイベント行をタップすると、そのイベントの配車画面（メイン）のURLへ遷移する

---

## 7. 依存タスク

- T21 イベント一覧（日付順・本日強調・過去グレーアウト）
- T38 行き・帰りタブ切り替え（状態管理・独立保持）

---

## 提案（タスク対象外）

なし
